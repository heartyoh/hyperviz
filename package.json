{
  "name": "hyperviz-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "clean": "yarn workspaces run clean"
  },
  "devDependencies": {
    "lerna": "^8.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
